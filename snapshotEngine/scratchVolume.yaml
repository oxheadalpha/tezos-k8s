apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: snapshot-cache-volume
  namespace: ""
spec:
  storageClassName: do-block-storage
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
