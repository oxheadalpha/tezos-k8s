apiVersion: v1
data:
  CHAIN_PARAMS: |
    {
      "activation_account": "{{ (index .Values.accounts 0).name }}",
      "bootstrap_peers": {{ toJson .Values.bootstrap_peers }},
      "chain_name": "{{ .Values.chain_name }}",
      "chain_type": "{{ .Values.chain_type }}",
      "defualt_bootstrap_mutez": "{{ .Values.protocol.bootstrap_mutez }}",
      "genesis_block": "{{ .Values.genesis.block }}",
      "is_old_accounts_parameter_format": {{ default false .Values.is_old_accounts_parameter_format }},
      "network": "{{ .Values.network }}",
      "nodes": {{ toJson .Values.nodes }},
      "proto_command": "{{ .Values.protocol.command }}",
      "protocol_hash": "{{ .Values.protocol.hash }}",
      "protocol_parameters": {{ toJson .Values.protocol_parameters }},
      "snapshot_url": "{{ .Values.snapshot_url }}",
      "timestamp": "{{ .Values.genesis.timestamp }}"
    }
  NODES: |
{{ .Values.nodes | toJson | indent 4}}
kind: ConfigMap
metadata:
  name: tezos-config
  namespace: {{ .Release.Namespace }}
---
apiVersion: v1
data:
  CHAIN_NAME: "{{ .Values.chain_name }}"
  NETWORK_ID: "{{ .Values.zerotier_config.zerotier_network }}"
  ZTAUTHTOKEN: "{{ .Values.zerotier_config.zerotier_token }}"
  NODES: |
{{ .Values.nodes | toJson | indent 4}}
kind: ConfigMap
metadata:
  name: zerotier-config
  namespace: {{ .Release.Namespace }}
---
apiVersion: v1
data:
  ACCOUNTS: |
{{ .Values.accounts | toJson | b64enc | indent 4 }}
kind: Secret
metadata:
  name: tezos-secret
  namespace: {{ .Release.Namespace }}
