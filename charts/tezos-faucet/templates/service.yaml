apiVersion: v1
kind: Service
metadata:
  labels:
    app: tezos-faucet
  name: tezos-faucet
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  ports:
    - name: frontend
      port: 80
      targetPort: 80
    - name: backend
      port: 3000
      targetPort: 3000
  selector:
    app: faucet
status:
  loadBalancer: {}
