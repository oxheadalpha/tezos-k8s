version: v1beta9
deployments:
  - name: backend
    kubectl:
      manifests:
        - ./backend.yaml
images:
  server:
    image: rpc-auth
dev:
  sync:
    - imageName: server
      labelSelector:
        app: rpc-auth
hooks:
  - command: minikube
    args:
      - addons
      - enable
      - ingress
    when:
      before:
        deployments: all
